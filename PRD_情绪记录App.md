# MoodLog（情绪猫）— 产品需求文档

> **文档版本**：v1.3.0
> **更新日期**：2026-09-04
> **文档状态**：已定稿
> **目标平台**：iOS 16.0+，iPhone
> **当前版本**：V1.3（Build 7）

## 1. 产品概述

MoodLog（情绪猫）是一款本地优先的情绪记录 App。用户可以快速记录情绪、强度、触发标签和备注，并通过日历、统计洞察与情绪卡片理解自己的情绪变化。

**产品定位**：一款情绪记录 App，帮你把感受变成清晰的自我认知。
**产品原则**：本地优先、隐私最小化、轻量记录、数据可携带。

## 2. 核心用户与场景

| 用户 | 核心诉求 |
| --- | --- |
| 情绪波动者 | 10 秒完成打卡，标记事件触发原因 |
| 自我成长者 | 查看趋势、分布和主导情绪，形成自我觉察 |
| 心理咨询辅助用户 | 导出 CSV/JSON，把记录带给咨询师 |

关键场景包括晨起或睡前记录、情绪波动时即时记录、日/周/月/季/年回顾，以及生成可保存或分享的情绪卡片。

## 3. 功能范围

### 3.1 首次引导

- 新用户分步了解 App 价值与本地隐私边界。
- 引导完成后进入首页打卡，不再重复展示。

### 3.2 情绪打卡

| 能力 | 说明 |
| --- | --- |
| 情绪选择 | 支持 8 类核心情绪：开心、难过、生气、焦虑、平淡、害怕、疲惫、放松 |
| 情绪强度 | 1-10 分滑块，默认 5 |
| 活动标签 | 预设标签 + 自定义标签；标签实体通过 Core Data 关系统一管理 |
| 备注 | 可选自由文本，记录触发事件与感受 |
| 保存 | 后台上下文异步写入，避免主线程阻塞 |
| 编辑 | 支持修改情绪、强度、标签与备注 |

标签只以 `ActivityTag` 关系作为唯一数据源；旧版字符串标签字段已移除。

### 3.3 记录与日历

- 按时间倒序浏览记录，支持分页加载。
- 月度日历展示情绪概览。
- 日情绪时间线展示情绪、强度、标签与备注。

### 3.4 数据洞察

洞察页提供今日、本周、本月、本季、年度五个区间，并与情绪卡片使用同一日历区间规则。

| 能力 | 说明 |
| --- | --- |
| 顶部摘要 | 展示“你的心情以 xx 为主”或“你的心情在 xx 和 xx 之间波动” |
| 情绪分布 | 统计各情绪次数与占比 |
| 标签统计 | 统计高频触发标签 |
| 记录总数 | 轻量 count 查询 |

### 3.5 情绪卡片

- 支持今日、本周、本月、本季、年度情绪卡片。
- 卡片展示日期区间、主导/波动叙述、情绪图标与记录统计。
- 分散情绪时展示前两名情绪图标和 `waveform.path.ecg` 波动符号。
- 支持保存到相册和系统分享。
- 图片渲染使用 iOS 16 `ImageRenderer`。
- 相册权限受限时提供权限引导，而不是只提示失败。

### 3.6 我的

| 模块 | 说明 |
| --- | --- |
| 情绪卡片入口 | 高可见度渐变卡片，快速进入分享生成页 |
| 情绪图书馆 | 以 200 本作为开馆目标；未开馆时点击显示剩余收录数量 |
| 数据导出 | CSV / JSON 本地导出 |
| 分享 App | 使用系统分享面板推荐应用 |
| 去 App Store 评分 | 跳转 App Store 评分页 |
| 问题反馈 | 内置二级页面加载技术支持网页 |
| 隐私协议 | 内置二级页面加载本地 HTML |

技术支持地址：`https://lt2058914895.github.io/moodlog/support.html`。

## 4. 数据与隐私

### 4.1 数据模型

| 实体 | 关键字段 |
| --- | --- |
| MoodRecord | id、moodType、intensity、note、createdAt、updatedAt、tags |
| ActivityTag | id、name、category、emoji、isCustom、usageCount、createdAt、lastUsedAt、records |

Core Data 启用自动轻量迁移。情绪记录与标签的关系是唯一数据源。

### 4.2 隐私边界

- 不接入第三方分析、广告、支付或云同步 SDK。
- 不采集用户身份信息。
- 情绪记录仅保存在设备本地。
- 仅使用“添加到照片库”权限保存情绪卡片。
- 用户可通过 CSV/JSON 导出数据，卸载 App 会删除本地数据。

## 5. 非功能要求

| 项目 | 要求 |
| --- | --- |
| 最低系统 | iOS 16.0 |
| 界面 | SwiftUI，iPhone 竖屏优先 |
| 存储 | Core Data + 本地缓存 |
| 性能 | 首页统计使用 count / fetchLimit，不加载全量记录 |
| 无障碍 | 关键控件具备 accessibility label |
| 兼容 | iOS 16.0 至当前最新 iOS 正式版本 |

## 6. App Store 交付项

| 项目 | 值 |
| --- | --- |
| Bundle Identifier | `lt.moodlog` |
| 显示名称 | 情绪猫 |
| App ID | `6791829175` |
| 营销版本 | 1.3 |
| 构建版本 | 7 |
| 出口合规 | 不使用非豁免加密，`ITSAppUsesNonExemptEncryption = NO` |
| 照片库说明 | 保存情绪卡片需要添加内容到您的相册 |
| 技术支持 | 提供公开网页入口与邮箱联系方式 |

## 7. 验收范围

### 7.1 功能验收

- 新用户可完成 onboarding；老用户不重复进入。
- 8 类情绪均可保存、编辑、删除。
- 标签可新增、复用，记录与标签关系正确。
- 五个统计区间日期一致。
- 情绪卡片可生成、保存、分享。
- CSV/JSON 导出内容完整。
- 分享 App、评分、技术支持、隐私协议跳转正常。

### 7.2 发布前检查

- Release 构建成功，无阻断错误。
- 本地化键 zh-Hans 与 en 数量一致。
- 不再引用成就系统、推送提醒、AI 洞察、社区、冥想、会员、Widget、Siri、HealthKit、iCloud 同步等未实现能力。
- App Icon、隐私政策、技术支持、版本号和权限说明完整。
